queue_rules:
  - name: default
    batch_size: 3
    merge_conditions:
      - check-success=gate
pull_request_rules:
  - name: queue PRs targeting main
    conditions:
      - base=main
    actions:
      queue:
        name: default
