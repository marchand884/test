pull_request_rules:
  - name: auto-squash PoC (explicit body template)
    conditions:
      - base=main
    actions:
      merge:
        method: squash
        commit_message_template: |
          {{ title }}

          {{ body }}
