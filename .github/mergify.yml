pull_request_rules:
  - name: ci-gate
    conditions:
      - base=main
    actions:
      post_check:
        title: "ci-gate satisfied by Mergify post_check"
        summary: "Emitted by Mergify, not real CI."
